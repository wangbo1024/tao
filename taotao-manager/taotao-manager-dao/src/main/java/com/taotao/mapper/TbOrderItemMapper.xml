<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taotao.mapper.TbOrderItemMapper">
    <insert id="insert">
        INSERT INTO tborderitem (id, itemId, orderId, num, title, price, totalFee, picPath)
          VALUE (#{id},#{itemId},#{orderId},#{num},#{title},#{price},#{totalFee},#{picPath})
    </insert>
</mapper>