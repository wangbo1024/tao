<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taotao.mapper.TbOrderMapper">
    <insert id="insert">
        INSERT INTO tborder (orderId, payment, paymentType, postFee, status, createTime, updateTime,userId)
          VALUE (#{orderId},#{payment},#{paymentType},#{postFee},#{status},#{createTime},#{updateTime},#{userId})
    </insert>

</mapper>